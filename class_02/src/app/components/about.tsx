function ABOUT(){
    return(
        <div>
            <h1>This is about page its directly export in pages.tsx file</h1>
            <h2>When we create div its means we can export more than one element from single return function</h2>
        </div>
    )
}
export default ABOUT;